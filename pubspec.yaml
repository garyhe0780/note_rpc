name: grpc_note_server
description: A gRPC-based note management API server
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  grpc: ^4.3.1
  protobuf: ^5.1.0
  fixnum: ^1.1.0
  uuid: ^4.0.0
  logging: ^1.3.0
  postgres: ^3.5.9

dev_dependencies:
  protoc_plugin: ^24.0.0
  test: ^1.24.0
  lints: ^6.0.0

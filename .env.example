# Server Configuration
GRPC_PORT=50051

# Storage Configuration
# Options: 'memory' or 'postgres'
STORAGE_TYPE=memory

# PostgreSQL Configuration (only needed if STORAGE_TYPE=postgres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=notes_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_MAX_CONNECTIONS=10
DB_USE_SSL=false
